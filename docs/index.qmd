---
title: "mathviz"
subtitle: "Themed charts, control panels and numerical helpers for interactive math pages"
toc: false
---

<!--
Copyright (c) 2026 Apurva Nakade. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Apurva Nakade
-->

mathviz is the machinery behind [Visual Math Lab](https://www.visualmathlab.com), packaged so any site can use it: a **Quarto extension** that drops into an OJS page with one line of front matter, and the same code as a **plain script bundle** for everything else.

Every chart, panel and slider on this site is drawn by the library itself. Toggle the theme in the navbar and watch them follow.

```{ojs}
//| echo: false
VM = window.VM
vmTheme = Generators.observe(notify => VM.plotting.onThemeChange(notify))
chartColors = VM.plotting.colors(vmTheme)
```

::: {.ojs-panel .ojs-row}
```{ojs}
//| echo: false
viewof heroF = Inputs.text({label: "f(x)", value: "sin(x) + x/3"})
```
```{ojs}
//| echo: false
viewof heroK = Inputs.range([0.5, 4], {label: "k", step: 0.05, value: 1})
```
:::

::: {.ojs-chart-block}
```{ojs}
//| echo: false
{
  const f = VM.expressions.makeFunction(math, heroF)
  const df = VM.expressions.makeDerivative(math, heroF)
  const xs = [], ys = [], dys = []
  for (let i = 0; i <= 400; i++) {
    const x = -6 + 12 * i / 400
    xs.push(x)
    ys.push(f ? f(heroK * x) : NaN)
    dys.push(df ? heroK * df(heroK * x) : NaN)
  }
  const div = document.createElement("div")
  div.className = "plotly-box-large"
  div.style.height = "380px"; div.style.minHeight = "0"
  Plotly.newPlot(div, [
    {x: xs, y: ys, name: "f(kx)", line: {color: chartColors.fn, width: 2.5}},
    {x: xs, y: dys, name: "d/dx f(kx)", line: {color: chartColors.alt, width: 1.5, dash: "dot"}}
  ], {
    xaxis: {title: "x"},
    legend: {x: 0.01, y: 0.99},
    annotations: f ? [] : VM.plotting.emptyState("Couldn't parse that expression")
  })
  VM.plotting.autoResize(div)
  return div
}
```
:::

## Quick start

::: {.panel-tabset}

## A new site

Copy the starter -- a complete Quarto website with seven working pages and the extension already installed:

```sh
git clone https://github.com/apurvanakade/mathviz
cp -r mathviz/starter my-site && cd my-site
quarto preview
```

Then copy any page (`index.qmd` is the smallest), rename it, and edit its cells. Its [README](https://github.com/apurvanakade/mathviz/tree/main/starter) lists what each page shows.

## An existing Quarto site

```sh
quarto add apurvanakade/mathviz
```

```yaml
# _quarto.yml
filters:
  - mathviz
```

Every page now has `VM`, math.js, Plotly and the stylesheet. Copy a page from the [starter](https://github.com/apurvanakade/mathviz/tree/main/starter) into your site to see it work.

:::

## What you get

- **Themed charts** -- Plotly and Observable Plot follow your site's light/dark toggle, with no per-page code.
- **Control panels** -- a few `ojs-*` classes lay Observable inputs out into a tidy panel; step sliders sit above the chart and get a play button.
- **A better modebar and a draggable legend** -- fullscreen, gentler zoom, pan by default; a floating legend that toggles traces.
- **Numerical helpers** -- safe expression parsing, ODE steppers, quadrature, fits, densities, seeded randomness.

## Next

| | |
|---|---|
| [Building a page](first-chart.qmd) | The starter's first page, cell by cell -- the pattern every page follows. |
| [Theming](theming.qmd) | Make the colors and fonts yours; how dark mode is detected. |
| [Markup](markup.qmd) | The `ojs-*` classes that lay out panels and charts. |
| [Installing](install.qmd) | Filter options, pinning a version, using the bundle on a non-Quarto page. |
| [API reference](reference/index.qmd) | Every `VM.*` function, with live examples. |
| [Troubleshooting](troubleshooting.qmd) | Symptom → cause → fix. |
