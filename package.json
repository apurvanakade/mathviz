{
  "name": "mathviz",
  "version": "0.1.2",
  "description": "Chart theming, control panels and numerical helpers for interactive math pages -- a Quarto extension and a plain script bundle.",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "repository": "github:apurvanakade/mathviz",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build && git diff --exit-code -- dist _extensions/mathviz/dist starter/_extensions"
  },
  "devDependencies": {
    "mathjs": "^15.2.0"
  }
}
